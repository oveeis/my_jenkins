pipeline{
    agent any
    stages{
      stage("build"){
          echo 'build the application'
          }
      }      
      stage("test"){
         echo 'test the application'
         } 
      }      
      stage("deploy"){
         echo 'deploy the application'
         } 
      }        
    }                
}            
